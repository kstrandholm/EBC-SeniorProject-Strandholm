<UserControl x:Class="DubuqueCodeCamp.Scheduler.AddSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">

  <StackPanel Margin="10">
    <DatePicker />
    <Calendar />
    <TextBox Text="{Binding TimeStart}"></TextBox>
    <TextBox Text="{Binding TimeEnd}"></TextBox>

    <Button HorizontalAlignment="Center" Command="{Binding UpdateSessionsCommand}">
      Save Session
    </Button>
  </StackPanel>

</UserControl>
