<UserControl x:Class="DubuqueCodeCamp.Scheduler.ScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
  <Grid>
    
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"></RowDefinition>
      <RowDefinition Height="*"></RowDefinition>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" FontWeight="Bold" Margin="0 0 0 5">Proposed Schedule</TextBlock>

    <ListView Grid.Row="1" ItemsSource="{Binding Scheudles, UpdateSourceTrigger=PropertyChanged}" ScrollViewer.VerticalScrollBarVisibility="Visible" >
      <ListView.View>
        <GridView>
          <GridViewColumn DisplayMemberBinding="{Binding }" Header="" Width="Auto" />
          <GridViewColumn DisplayMemberBinding="{Binding }" Header="" Width="Auto" />
          <GridViewColumn DisplayMemberBinding="{Binding }" Header="" Width="Auto" />
          <GridViewColumn DisplayMemberBinding="{Binding }" Header="" Width="Auto" />
        </GridView>
      </ListView.View>
    </ListView>

  </Grid>
</UserControl>
